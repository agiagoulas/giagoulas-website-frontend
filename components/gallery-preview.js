import GalleryImage from './image';

export default function GalleryPreview({ images }) {
    return (
        <>
            {/* {images.map((image) =>
                <GalleryImage src={image} text="hello" key={image} />
            )} */}
            <p>Todo Preview</p>
        </>
    );
}
